function Level(background) {
	this.background = background;
}	

Level.prototype.create = function () {
	// Initialize all of the shit that has to go into making a level.
	// Is this necessary?
};

Level.prototype.scrollBG = function () {
	// Scroll the background downward at a specific duration.
};